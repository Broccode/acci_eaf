# Gradle configuration for ACCI EAF
org.gradle.jvmargs=-Xmx8g -XX:MaxMetaspaceSize=1g
org.gradle.daemon=true
org.gradle.caching=true
org.gradle.parallel=true

# Java configuration
# org.gradle.java.home=/Library/Java/JavaVirtualMachines/amazon-corretto-21.jdk/Contents/Home
# Note: Java home is configured per environment. CI uses setup-java action.

# Disable Kotlin Nullability plugin temporarily to avoid OpenAPI generation crash (Hilla issue #3443)
# vaadin.nonnull-plugin.enabled=false

# Pass system property to disable Vaadin Kotlin Nullability plugin globally
# org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=512m -Dvaadin.nonnull-plugin.enabled=false
